config → openit/utils/clashcheck/config/config.yaml
测活只在 CI 或明确要求的环境跑。本地不要扫节点。

=== Local Python 3.x
$ git clone https://github.com/Acheirial/openit.git --single-branch --depth=1
$ cd openit/utils/clashcheck && ./fetch-mihomo.sh && ./start.sh
=== Docker Containers
$ git clone https://github.com/Acheirial/openit.git --single-branch --depth=1
$ docker build -t clashcheck openit/utils/clashcheck
$ docker run -d --restart=on-failure:3 -p 80:80 clashcheck

== Check
=== Web
→ http://0.0.0.0:80
=== Terminal
$ curl 0.0.0.0:80/README

== Use
=== Web
→ http://0.0.0.0:80 → check.yaml
=== Terminal
$ open openit/utils/clashcheck/data/check.yaml

https://github.com/Acheirial/openit#Dockerfile
